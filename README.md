```
DONE:  
    -make tracking more efficient (Summer of GOTO?)
    -add per role command whitelist?
        added blacklist on the bot itself per user
    -implement better permission check
    -rewrite old messy commands (like move holy shit what was I doing)
    -create common function that caches mongo profile for each guild

TODO:
    -rewrite help command to work variably
    -add per command embeds for help command
    -implement cooldowns
    -add roles to blacklist
    -finish video.js
    -fix leaderboard?
```
